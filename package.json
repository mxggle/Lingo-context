{
    "name": "lingo-context",
    "version": "1.0.0",
    "description": "Chrome Extension for English/Japanese learners",
    "scripts": {
        "dev": "node hot-reload.js",
        "build:css": "npx tailwindcss -i ./src/input.css -o ./styles.css --minify",
        "watch:css": "npx tailwindcss -i ./src/input.css -o ./styles.css --watch",
        "package": "npm run build && cd dist && zip -r ../extension.zip .",
        "build": "rm -rf dist && mkdir dist && cp manifest.json background.js content.js popup.js dashboard.js config.js db-hook.js styles.css popup.html dashboard.html dist/ && cp -r icons dist/ && cp README.md dist/"
    },
    "devDependencies": {
        "tailwindcss": "^3.4.0"
    }
}